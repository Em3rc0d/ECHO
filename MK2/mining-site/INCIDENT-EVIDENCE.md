# Incident Evidence — MK2

Cada incidente operacional relevante registra:

`incident_id`, build/model, affected sources, detection signal, timeline, impact, root cause, mitigation, permanent fix, tests added y artifacts/log hashes.

Un incidente debe alimentar regression tests o risk register cuando revele una failure mode nueva.